import react, { useContext } from "Ract";


const store = {
    show: false
};

let conaText = useContext(reduser);

let reduser = useReduser(store)

conaText.producser = () => {

useEffect(() => {
axios.fetch().then(res => {
})
}

 return  { store , dispatch };
}


return (    
conaText.conSumer